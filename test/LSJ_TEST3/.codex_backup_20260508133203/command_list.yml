---
version_check_commands:
  - name: redirfs_version # 보고서 이름
    display: cat /sys/module/redirfs/version
    run: cat /sys/module/redirfs/version

  - name: redhat_release
    display: cat /etc/redhat-release
    run: cat /etc/redhat-release

  - name: host_ip
    display: hostname -I
    run: hostname -I


x_commands:
  # - "hostname -I"